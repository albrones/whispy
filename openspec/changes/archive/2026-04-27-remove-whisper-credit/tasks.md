## 1. Implement credit stripping function

- [x] 1.1 Add `strip_whisper_credit()` function to `src/whispy/core/audio.py` with regex pattern for French and English credit variants
- [x] 1.2 Apply the function in `AudioEngine.transcribe()` after joining text segments and before returning

## 2. Add unit tests

- [x] 2.1 Create test file `tests/test_text_cleaning.py` (or add to existing test module)
- [x] 2.2 Add test: French credit "Sous-titres réalisés par Whisper" is stripped
- [x] 2.3 Add test: French credit "Sous-titres réalisées par" is stripped
- [x] 2.4 Add test: English credit "Subtitles generated by Whisper" is stripped
- [x] 2.5 Add test: Text without credit is returned unchanged
- [x] 2.6 Add test: Credit with trailing whitespace is handled correctly
- [x] 2.7 Add test: Text that only contains a credit (no additional content) returns empty string

## 3. Update specs and verify

- [x] 3.1 Verify `specs/text-cleaning/spec.md` has all required scenarios
- [x] 3.2 Verify `specs/core-engine/spec.md` delta spec is correct
- [x] 3.3 Run existing tests to ensure no regressions
