## 1. Update research-log skill frontmatter

- [x] 1.1 Add `trigger: always` to `.agents/skills/research-log/SKILL.md` YAML frontmatter

## 2. Update AGENTS.md skill loading logic

- [x] 2.1 Document the `trigger: always` behavior in the auto-discovery section
- [x] 2.2 Add logic description for how `trigger: always` bypasses keyword filtering
